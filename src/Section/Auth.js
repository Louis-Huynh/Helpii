import React from "react";
import styled from "styled-components";

const Auth = () => {
  return (
    <Wrapper>
      <p>auth</p>
    </Wrapper>
  );
};

export default Auth;

const Wrapper = styled.div``;
