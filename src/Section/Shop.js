import React from "react";
import styled from "styled-components";

const Shop = () => {
  return (
    <Wrapper>
      <p>Shop</p>
    </Wrapper>
  );
};

export default Shop;

const Wrapper = styled.div``;
